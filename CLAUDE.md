# 고구마마켓 (GGM)

중고거래 웹사이트를 만드는 **개발 연습용 프로젝트**입니다.
작업자는 비개발자이며, 개발을 배우는 중입니다.

## 기술 구성

| 역할 | 사용하는 것 | 쉬운 설명 |
| --- | --- | --- |
| 화면 만들기 | Next.js (React) | 사용자가 보는 웹페이지를 만드는 도구 |
| 데이터 보관 | Supabase | 회원정보·상품정보·사진을 저장해 두는 창고 |
| 인터넷 공개 | Vercel | 내 컴퓨터의 코드를 인터넷 주소로 띄워주는 서비스 |

## 작업 규칙 (반드시 지킬 것)

### 1. 개발용어는 쉽게 풀어서 설명한다
- 전문용어를 쓸 때는 항상 괄호로 쉬운 뜻을 덧붙인다.
  - 예) "마이그레이션(데이터 창고의 구조를 바꾸는 작업)"
  - 예) "환경변수(비밀번호처럼 코드에 직접 적으면 안 되는 값을 따로 보관하는 곳)"
- 영어 약자를 그대로 던지지 않는다. (RLS → "RLS(내 데이터는 나만 보이게 막아주는 잠금장치)")

### 2. 지금 무엇을 하는지 자주 말한다
- 작업을 시작할 때: "지금부터 ○○를 만들겠습니다"
- 작업 중간중간: "○○ 끝났고, 이제 ○○ 합니다"
- 작업이 끝나면: "○○ 완료. 바뀐 파일은 △△입니다"
- 오래 걸리는 작업은 조용히 진행하지 말고 중간 상황을 알린다.

### 3. 사용자가 지시한 것만 단계적으로 수행한다
- 요청한 범위 **딱 그만큼만** 만든다. 미리 앞서가지 않는다.
- "다음 단계로 이런 게 필요해 보인다"는 제안만 하고, 허락 없이 만들지 않는다.
- 한 번에 여러 기능을 몰아서 구현하지 않는다. 한 단계 → 확인 → 다음 단계.

### 4. 할 수 있는 건 최대한 직접 처리한다
- 코드 작성, 파일 수정, 폴더 정리, DB 구조 변경(마이그레이션 작성/적용),
  타입 생성, 빌드 확인 등은 **묻지 말고 직접** 한다.
- 사람이 직접 해야만 하는 일(아래)만 골라서 안내한다.
  - Supabase / Vercel / GitHub 웹사이트에서 버튼 누르기
  - 비밀키·비밀번호 입력
  - 결제, 도메인 구입
  - 이메일 인증 클릭
- 안내할 때는 **클릭 순서를 번호로** 적는다.
  - 예) 1) supabase.com 접속 → 2) 왼쪽 메뉴 Settings → 3) API 탭 → 4) `anon key` 복사

### 5. 개발을 마치면 바로 확인할 수 있게 한다
- 코드 수정이 끝나면 **개발 서버를 직접 띄우고 화면을 확인한 뒤**, 스크린샷이나 결과를 보여준다.
- 사용자에게 "직접 확인해 보세요"라고만 말하고 끝내지 않는다.
- 확인할 주소를 명확히 알려준다. (예: http://localhost:3000/products/new)
- 에러가 있으면 숨기지 말고 그대로 알리고, 원인과 해결책을 쉬운 말로 설명한다.

## 개발 서버 실행 방법

`.claude/launch.json`에 `goguma-market` 설정이 있으므로, preview_start 도구로 실행한다.
터미널에서 `npm run dev`를 직접 돌리지 않는다.

주소: http://localhost:3000

## 이 프로젝트만의 주의사항

- **Supabase 프로젝트를 가계부 앱과 함께 쓴다.** 그래서 이 프로젝트가 만드는 테이블·함수 이름에는
  반드시 `ggm_` 접두사를 붙인다. (예: `ggm_products`, `ggm_profiles`)
  다른 앱 테이블은 절대 건드리지 않는다.
- `.env.local` 파일에는 비밀키가 들어 있다. **깃(git)에 올리지 않는다.**
  새 항목이 생기면 `.env.example`에도 (가짜 값으로) 같이 추가한다.
- DB 구조를 바꿀 때는 `supabase/migrations/` 폴더에 SQL 파일로 남긴다.
  (나중에 무엇을 바꿨는지 기록으로 남기기 위함)

## Next.js 버전 안내

@AGENTS.md
